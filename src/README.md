# src/ — Prism builder infrastructure (Python)

Intent -> mechanism. IR model + validator (ir/), wire codec (wire/), delivery shapes (shapes/), per-language generators (gen/), golden corpus generator (corpus/).
